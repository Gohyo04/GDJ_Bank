<?xml version="1.0" encoding="UTF-8"?>
<!-- 스키마주소 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <!-- mapper namespace는 비우면 에러 -->
  <mapper namespace="com.gohyo.app.product.ProductDAO">
  
  	<select id="getTotal" resultType="java.lang.Long">
		SELECT COUNT(PRODUCTNUM) FROM PRODUCT   	
	</select>
  
  	<select id="getList" resultType="ProductDTO" parameterType="Pager">
  		SELECT * FROM
			(
			SELECT ROWNUM R, RG.* FROM
				(
				SELECT * FROM PRODUCT
				ORDER BY PRODUCTNUM DESC
				) RG
			)
			WHERE R BETWEEN #{startRow} AND #{lastRow}
  	</select>
  	
  	<select id="getDetail" resultMap="getDetailResult" parameterType="ProductDTO">
  		SELECT * 
		FROM PRODUCT
		LEFT OUTER JOIN PRODUCTFILES
		USING(PRODUCTNUM)
		WHERE PRODUCTNUM = #{productNum}
  	</select>
  	<resultMap type="ProductDTO" id="getDetailResult">
  		<!-- PK -->
  		<id column="PRODUCTNUM" property="productNum"/>
  		<!-- 나머지 -->
  		<result column="PRODUCTNAME" property="productName"/>
  		<result column="PRODUCTCONTENTS" property="productContents"/>
  		<result column="PRODUCTRATE" property="productRate"/>
  		<result column="PRODUCTJUMSU" property="productJumsu"/>
<!--   		<association property="productFileDTO" javaType="ProductFileDTO">
  			PK
  			<id column="PRODUCTNUM" property="productNum"/>
  			그외
  			<result column="FILENAME" property="fileName"/>
  			<result column="ORINAME" property="oriName"/>
  		</association> -->
   	 	<collection property="productFileDTOs" javaType="List" ofType="productFileDTO">
  			<!-- PK --> 
   			<id column="PRODUCTNUM" property="productNum"/>
  			<!-- 그외 --> 
   			<result column="FILENAME" property="fileName"/> 
  			<result column="ORINAME" property="oriName"/>
  		</collection>
  	</resultMap>
  	
	<!-- File List 1:N -->
	<select id="getListFiles" resultType="ProductFileDTO" parameterType="ProductDTO">
		SELECT * FROM PRODUCTFILES
		WHERE PRODUCTNUM = #{productNum}
	</select>
  	
  	
  	<insert id="add" parameterType="ProductDTO">
  		<selectKey keyProperty="productNum" order="BEFORE" resultType="Long">
			  SELECT NO_PK.NEXTVAL FROM DUAL 
  		</selectKey>
  		INSERT INTO PRODUCT 
  		VALUES
  		(
  			#{productNum},
  			#{productName},
  			#{productContents},
  			#{productRate},
  			#{productJumsu}
		)
  	</insert>
  	
  	<insert id="addFile" parameterType="ProductFileDTO">
  		INSERT INTO PRODUCTFILES (PRODUCTNUM, FILENUM, FILENAME, ORINAME)
		VALUES (#{productNum}, PF_SEQ.NEXTVAL, #{fileName}, #{oriName})
  	</insert>
  	
  	<delete id="delete" parameterType="ProductDTO">
  		DELETE PRODUCT WHERE PRODUCTNUM = #{productNum}
  	</delete>
  </mapper>